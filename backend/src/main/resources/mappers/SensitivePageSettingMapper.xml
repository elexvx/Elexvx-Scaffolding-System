<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.tdesign.mapper.SensitivePageSettingMapper">
  <sql id="BaseSelect">
    SELECT id,
      page_key,
      page_name,
      enabled,
      created_at,
      updated_at
    FROM sensitive_page_settings
  </sql>

  <select id="selectAll" resultType="com.tencent.tdesign.entity.SensitivePageSetting">
    <include refid="BaseSelect" />
  </select>

  <select id="selectByPageKey" resultType="com.tencent.tdesign.entity.SensitivePageSetting">
    <include refid="BaseSelect" />
    WHERE page_key = #{pageKey}
    <choose>
      <when test="_databaseId == 'sqlserver'">
        OFFSET 0 ROWS FETCH NEXT 1 ROWS ONLY
      </when>
      <when test="_databaseId == 'oracle'">
        FETCH FIRST 1 ROWS ONLY
      </when>
      <otherwise>
        LIMIT 1
      </otherwise>
    </choose>
  </select>

  <select id="selectById" resultType="com.tencent.tdesign.entity.SensitivePageSetting">
    <include refid="BaseSelect" />
    WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="com.tencent.tdesign.entity.SensitivePageSetting" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO sensitive_page_settings (
      page_key,
      page_name,
      enabled,
      created_at,
      updated_at
    )
    VALUES (
      #{pageKey},
      #{pageName},
      #{enabled},
      #{createdAt},
      #{updatedAt}
    )
  </insert>

  <update id="update" parameterType="com.tencent.tdesign.entity.SensitivePageSetting">
    UPDATE sensitive_page_settings
    SET
      page_key = #{pageKey},
      page_name = #{pageName},
      enabled = #{enabled},
      updated_at = #{updatedAt}
    WHERE id = #{id}
  </update>
</mapper>
