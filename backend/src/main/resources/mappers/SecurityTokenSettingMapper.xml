<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.tdesign.mapper.SecurityTokenSettingMapper">
  <select id="selectTop" resultType="com.tencent.tdesign.entity.SecurityTokenSetting">
    SELECT id, session_timeout_minutes, token_timeout_minutes, token_refresh_grace_minutes, allow_url_token_param
    FROM security_token_settings
    ORDER BY id ASC
    <choose>
      <when test="_databaseId == 'sqlserver'">
        OFFSET 0 ROWS FETCH NEXT 1 ROWS ONLY
      </when>
      <when test="_databaseId == 'oracle'">
        FETCH FIRST 1 ROWS ONLY
      </when>
      <otherwise>
        LIMIT 1
      </otherwise>
    </choose>
  </select>

  <insert id="insert" parameterType="com.tencent.tdesign.entity.SecurityTokenSetting" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO security_token_settings (
      session_timeout_minutes, token_timeout_minutes, token_refresh_grace_minutes, allow_url_token_param
    ) VALUES (
      #{sessionTimeoutMinutes}, #{tokenTimeoutMinutes}, #{tokenRefreshGraceMinutes}, #{allowUrlTokenParam}
    )
  </insert>

  <update id="update" parameterType="com.tencent.tdesign.entity.SecurityTokenSetting">
    UPDATE security_token_settings
    SET
      session_timeout_minutes = #{sessionTimeoutMinutes},
      token_timeout_minutes = #{tokenTimeoutMinutes},
      token_refresh_grace_minutes = #{tokenRefreshGraceMinutes},
      allow_url_token_param = #{allowUrlTokenParam}
    WHERE id = #{id}
  </update>
</mapper>
