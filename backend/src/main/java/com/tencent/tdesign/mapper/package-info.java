/**
 * MyBatis Mapper 接口定义。
 *
 * <p>该包仅负责数据库读写映射（接口 + XML SQL），不应包含业务逻辑。重要约定：
 * <ul>
 *   <li>方法签名与 XML 中参数/返回映射保持一致，避免“字段名不一致导致空值”的隐性问题。</li>
 *   <li>复杂 SQL 建议在 XML 中按块拆分并添加注释，提升可维护性。</li>
 * </ul>
 */
package com.tencent.tdesign.mapper;

